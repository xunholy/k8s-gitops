apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kube-system

resources:
  - helmrelease.yaml
  - virtualservice.yaml

configMapGenerator:
  - name: cilium-values
    files:
      - values.yaml=../install/1.12.5.yaml

configurations:
  - kustomizeconfig.yaml
