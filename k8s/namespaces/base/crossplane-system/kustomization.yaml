apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: crossplane-system

resources:
  - helmrelease.yaml
  - namespace.yaml
  # TODO: Currently disabled due to it breaking the controlplane installing 200+ CRDs
  # - provider.yaml
  - secret.enc.yaml
