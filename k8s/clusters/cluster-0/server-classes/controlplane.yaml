---
apiVersion: metal.sidero.dev/v1alpha1
kind: ServerClass
metadata:
  name: serverclass-controlplane
spec:
  selector:
    matchLabels:
      controlplane: "true"
    matchExpressions:
      - key: environment
        operator: NotIn
        values:
          - prod
  configPatches:
    - op: replace
      path: /machine/install/disk
      value: /dev/sda
    - op: replace
      path: /cluster/proxy/disabled
      value: true
