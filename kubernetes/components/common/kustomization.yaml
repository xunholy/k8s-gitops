---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# NOTE: Alerts temporarily disabled for bootstrap (ExternalSecrets CRDs don't exist yet)
# Once external-secrets is deployed via Flux, this can be re-enabled
# resources:
#   - ./alerts

# Placeholder to keep kustomization valid (empty components fail validation)
labels:
  - pairs:
      component: common
