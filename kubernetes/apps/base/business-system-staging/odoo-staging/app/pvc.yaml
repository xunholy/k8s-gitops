---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: odoo-filestore-staging
  namespace: business-system-staging
  labels:
    app.kubernetes.io/name: odoo-staging
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ceph-block
