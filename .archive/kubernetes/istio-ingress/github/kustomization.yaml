---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - destinationrule.yaml
  # TODO: Re-enable when switching to Gateway API for Istio ingress
  # - httproute.yaml
  - service.yaml
  - serviceentry.yaml
  - virtualservice.yaml
